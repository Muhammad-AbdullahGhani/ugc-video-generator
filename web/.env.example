# Google Gemini API Key (Required for UGC script blueprint & chat intelligence)
GEMINI_API_KEY=your_gemini_api_key_here

# Tenor API Key (Optional - if omitted, built-in curated reaction GIFs are used)
TENOR_API_KEY=your_tenor_api_key_here

# FFmpeg Executable Path (Optional - defaults to @ffmpeg-installer or system PATH)
# FFMPEG_PATH=/usr/bin/ffmpeg
